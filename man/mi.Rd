% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mi.R
\name{mi}
\alias{mi}
\title{Estimate Mutual Information (MI) between two variables}
\usage{
mi(x, y, method = c("binning", "knn"), bins = 10, k = 3, ...)
}
\arguments{
\item{x}{Numeric vector (first variable).}

\item{y}{Numeric vector (second variable).}

\item{method}{Estimation method: \code{"binning"} (default) or \code{"knn"}.}

\item{bins}{Number of bins per dimension for the binning method (default 10).}

\item{k}{Number of nearest neighbors for the kNN method (default 3).}

\item{...}{Additional arguments (currently unused).}
}
\value{
A numeric MI value in nats (natural log units).
}
\description{
Computes the mutual information I(X; Y) using either a binning-based
histogram estimator or a k-nearest-neighbor (KSG) estimator.
}
\examples{
set.seed(42)
x <- rnorm(100)
y <- x + rnorm(100, 0, 0.5)
mi(x, y)
mi(x, y, method = "knn", k = 5)

}
\references{
Kraskov, Stögbauer & Grassberger (2004) Phys. Rev. E 69:066138.
}
