% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{pmi_network}
\alias{pmi_network}
\title{Infer a direct association network using Part Mutual Information}
\usage{
pmi_network(
  data,
  method = c("binning", "knn"),
  bins = 5,
  k = 3,
  threshold = 0.05,
  n_permutations = 100,
  adjust = c("fdr", "bonferroni", "none"),
  mc_cores = 1,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Numeric matrix or data.frame (rows = samples, columns = variables).
Variable/column names are used as node labels.}

\item{method}{PMI estimation method: \code{"binning"} or \code{"knn"}.}

\item{bins}{Number of bins for binning estimator (default 5).}

\item{k}{Number of neighbors for kNN estimator (default 3).}

\item{threshold}{Significance threshold for p-values (default 0.05).}

\item{n_permutations}{Number of permutations for significance testing
(default 100). Use at least 1000 for published results.}

\item{adjust}{Method for multiple testing correction:
\code{"none"}, \code{"bonferroni"}, or \code{"fdr"} (default \code{"fdr"}).}

\item{mc_cores}{Number of cores for parallel computation (default 1).
Windows users should use 1.}

\item{verbose}{Print progress messages (default TRUE).}

\item{...}{Additional arguments passed to \code{\link{pmi}}.}
}
\value{
A list with components:
\item{adjacency}{Adjacency matrix (binary, 0/1) of significant edges.}
\item{pmi_matrix}{Matrix of PMI values for all pairs.}
\item{pvalue_matrix}{Matrix of permutation p-values.}
\item{pvalue_adjusted}{Matrix of adjusted p-values.}
}
\description{
Constructs a network where edges represent direct associations between
variables. For each pair (i, j), PMI is computed conditioning on all other
variables (full-order conditioning) or on a specified subset. Statistical
significance is assessed via permutation testing.
}
\examples{
set.seed(42)
n <- 50
g <- rnorm(n)
x <- g + rnorm(n, 0, 0.5)
y <- g + rnorm(n, 0, 0.5)
z <- x + rnorm(n, 0, 0.3)
w <- rnorm(n)
dat <- data.frame(X = x, Y = y, Z = z, W = w)
net <- pmi_network(dat, n_permutations = 20, threshold = 0.1)
print(net$adjacency)

}
\references{
Zhao et al. (2016) PNAS 113(18):5130-5135.
}
